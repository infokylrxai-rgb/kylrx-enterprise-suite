{
  "AllowMultiple": "false",
  "Description": "\"Implement interactive loadInsights, openInsightDetail, and showToast functions in admin onboarding AI html.\"",
  "EndLine": "828",
  "Instruction": "\"Replace static loadInsights function with the interactive loadInsights, openInsightDetail, and showToast functions.\"",
  "ReplacementContent": "\"        async function loadInsights() {\\n            const container = document.getElementById('insightsContainer');\\n            try {\\n                const snap = await getDocs(collection(db, 'intelligence_insights'));\\n                if (!snap.empty) {\\n                    container.innerHTML = '';\\n                    snap.forEach(doc => {\\n                        const data = doc.data();\\n                        const item = document.createElement('div');\\n                        item.className = `insight-item ${data.type === 'bottleneck' ? 'bottleneck-card' : ''}`;\\n                        const icon = data.type === 'bottleneck' ? 'alert-triangle' : (data.type === 'reminder' ? 'trending-up' : 'zap');\\n                        const color = data.type === 'bottleneck' ? '#ef4444' : (data.type === 'reminder' ? '#f59e0b' : 'var(--primary)');\\n                        \\n                        item.innerHTML = `\\n                            <div class=\\\"insight-icon\\\" style=\\\"color: ${color};\\\"><i data-lucide=\\\"${icon}\\\"></i></div>\\n                            <div>\\n                                <h4 style=\\\"font-size: 0.9rem; font-weight: 800;\\\">${data.title}</h4>\\n                                <p style=\\\"font-size: 0.8rem; color: var(--text-muted);\\\">${data.description}</p>\\n                            </div>\\n                        `;\\n                        item.onclick = () => {\\n                            openInsightDetail(data);\\n                        };\\n                        container.appendChild(item);\\n                    });\\n                    if (window.lucide) lucide.createIcons();\\n                }\\n            } catch (err) {\\n                console.error(\\\"Insights load fault:\\\", err);\\n            }\\n        }\\n\\n        function showToast(message) {\\n            const toast = document.getElementById('successToast');\\n            if (!toast) return;\\n            toast.querySelector('span').textContent = message;\\n            toast.classList.add('active');\\n            setTimeout\n<truncated 12498 bytes>",
  "StartLine": "796",
  "TargetContent": "\"        async function loadInsights() {\\n            const container = document.getElementById('insightsContainer');\\n            try {\\n                const snap = await getDocs(collection(db, 'intelligence_insights'));\\n                if (!snap.empty) {\\n                    container.innerHTML = '';\\n                    snap.forEach(doc => {\\n                        const data = doc.data();\\n                        const item = document.createElement('div');\\n                        item.className = `insight-item ${data.type === 'bottleneck' ? 'bottleneck-card' : ''}`;\\n                        const icon = data.type === 'bottleneck' ? 'alert-triangle' : (data.type === 'reminder' ? 'trending-up' : 'zap');\\n                        const color = data.type === 'bottleneck' ? '#ef4444' : (data.type === 'reminder' ? '#f59e0b' : 'var(--primary)');\\n                        \\n                        item.innerHTML = `\\n                            <div class=\\\"insight-icon\\\" style=\\\"color: ${color};\\\"><i data-lucide=\\\"${icon}\\\"></i></div>\\n                            <div>\\n                                <h4 style=\\\"font-size: 0.9rem; font-weight: 800;\\\">${data.title}</h4>\\n                                <p style=\\\"font-size: 0.8rem; color: var(--text-muted);\\\">${data.description}</p>\\n                            </div>\\n                        `;\\n                        container.appendChild(item);\\n                    });\\n                    if (window.lucide) lucide.createIcons();\\n                }\\n            } catch (err) {\\n                console.error(\\\"Insights load fault:\\\", err);\\n            }\\n        }\"",
  "TargetFile": "\"c:\\\\Users\\\\Admin\\\\OneDrive\\\\Desktop\\\\product_sprint\\\\admin-onboarding-ai.html\"",
  "toolAction": "\"Update JavaScript logic\"",
  "toolSummary": "\"Replace static insights logic\""
}